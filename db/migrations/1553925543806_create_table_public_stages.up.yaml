- args:
    sql: CREATE TABLE "public"."stages"("stage_id" text NOT NULL UNIQUE, "name" text
      NOT NULL, PRIMARY KEY ("stage_id") );
  type: run_sql
- args:
    name: stages
    schema: public
  type: add_existing_table_or_view
